# Docker airflow examples

- this docker starts with new configuration and samples for running inside airflow 2.4.3, to run this container use:
```
docker-compose up
```
this will start the server in port 8080

